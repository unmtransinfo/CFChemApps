version: '3.8'

services:
  app:
    build:
      context: ./
    ports:
    - "5000:5000"
    # command: python3 manage.py runserver 0.0.0.0:5000
    image: cfchemapps:latest
    volumes:
      - .:/cfchemapps
    env_file:
      - ./.env.dev